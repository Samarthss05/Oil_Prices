# No API key needed for the cooking-oil slice. Local extraction is the default.
# Paid historical backfill remains disabled until human evaluation passes.
LLM_MONTHLY_CAP_USD=50
PAID_HISTORICAL_BACKFILL=false
